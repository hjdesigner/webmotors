'use strict'
import React from 'react'

const ButtonClean = ({ handleClean }) => (
  <button className='search-footer__buttons--clean' onClick={handleClean}>Limpar filtros</button>
)

export default ButtonClean
